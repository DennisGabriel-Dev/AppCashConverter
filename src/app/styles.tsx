import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  container: {
    paddingLeft: 15,
    paddingTop: 15,
    display: "flex"
  },

  input: {
    borderColor: 'black',
    borderRadius: 8,
    borderWidth: 1,
    width: '80%'
  }
})